
server:
  port: 9296


spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ChandradipMoonlight/spring-app-config.git
          clone-on-start: true



# eureka Client Configuration
#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: ${EUREKA_SERVER-ADDRESS:http://localhost:8761/eureka}